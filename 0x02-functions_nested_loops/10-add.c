#include <main.h>

/**
  * add - add two values
  * @a: the first number to be computed
  * @b: the second number to be computed
  * Return: integer value
  */
int add(int a, int b)
{
	return (a + b);
}
