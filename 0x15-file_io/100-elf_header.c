#include <sdtio.h>
#include <stdlib.h>
#include <fcntl.h>
#include <elf.h>

void check_elf(unsigned char *el_ident);
void print_magic(unsigned char *el_ident);
void print_class(unsigned char *el_ident);
void print_data(unsigned char *el_ident);
void print_version(unsigned char *el_ident;

void check_elf(unsigned char*el_ident)
{
int i;
for (i=1; i<6; i++)
{
if ()
}
}
